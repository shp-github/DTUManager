# HTTP 升级 API 使用说明

应用启动后自动在 `8080` 端口提供 HTTP 服务，外部系统可直接调用接口触发设备 OTA 升级。

---

## POST /api/upgrade

触发指定设备的固件升级，接口会阻塞等待设备上报升级结果后返回。

### 请求

```http
POST http://<本机IP>:8080/api/upgrade
Content-Type: application/json
```

```json
{
  "deviceId": "B48A0AB2FF7B",
  "firmwarePath": "D:\\work\\wt32-arduino-dtu\\.pio\\build\\wt32-eth01\\firmware.bin"
}
```

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `deviceId` | string | ✅ | 设备号（UDP 自动发现的设备 ID，可在应用界面查看） |
| `firmwarePath` | string | ✅ | 本地固件文件绝对路径（`.bin` 文件） |

### 处理流程

1. 校验参数 + 检查本地固件文件是否存在
2. 将固件从本地路径拷贝到 `files/` 目录（供设备 HTTP 下载）
3. 通过 `deviceId` 从 UDP 设备列表查找设备 IP
4. UDP 发送升级命令到设备
5. MQTT 推送 OTA 通知到 `/server/cmd/{deviceId}`
6. 阻塞监听设备 MQTT 进度上报 `/dev/ota/{deviceId}`（最长等待 120 秒）
7. 返回最终结果

### 设备上报的 OTA 终态

| 状态值 | 含义 | 接口行为 |
|---|---|---|
| `success` | 升级成功 | 返回成功 |
| `completed` | 升级完成 | 返回成功 |
| `error` | 升级失败 | 返回失败 |
| `failed` | 升级失败 | 返回失败 |
| 超时（120s） | 未收到终态 | 返回超时 |

> 中间状态如 `downloading`、`writing`、`installing` 等不会终止等待。

### 成功响应

```json
{
  "success": true,
  "downloadUrl": "http://192.168.0.104:8080/download/firmware.bin",
  "status": "success",
  "progress": 100,
  "message": "设备 B48A0AB2FF7B 升级完成"
}
```

### 失败响应

设备不在线：

```json
{
  "success": false,
  "error": "未找到设备: B48A0AB2FF7B，请确认设备已在线"
}
```

固件文件不存在：

```json
{
  "success": false,
  "error": "固件文件不存在: D:\\work\\firmware.bin"
}
```

升级超时：

```json
{
  "success": false,
  "downloadUrl": "http://192.168.0.104:8080/download/firmware.bin",
  "status": "timeout",
  "progress": 0,
  "error": "等待设备升级结果超时 (120秒)"
}
```

---

## GET /api/status

查看服务状态。

```json
{
  "fileServer": true,
  "upgradeHandler": true,
  "port": 8080,
  "filesDir": "/path/to/files",
  "firmwareFiles": ["firmware.bin"]
}
```

---

## curl 示例

```bash
# Windows (PowerShell)
curl -X POST http://192.168.0.104:8080/api/upgrade `
  -H "Content-Type: application/json" `
  -d '{"deviceId":"B48A0AB2FF7B","firmwarePath":"D:\\work\\firmware.bin"}'

# Linux / macOS
curl -X POST http://192.168.0.104:8080/api/upgrade \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"B48A0AB2FF7B","firmwarePath":"/home/user/firmware.bin"}'
```
